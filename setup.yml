---
--name:basic server setup
hosts: local
become: yes # use privilage escalation(sudo)
tasks:
- name: update apt cache
apt:
update_cache: yes
- name: install curl
apt:
name: curl
    state: present
 
